# Check if the current code is being executed in rstudio console or from a console outside rstudio

Check if the current code is being executed in rstudio console or from a
console outside rstudio

## Usage

``` r
.is_running_in_console()
```

## Value

TRUE is being run in console FALSE if being knitted, or being run
