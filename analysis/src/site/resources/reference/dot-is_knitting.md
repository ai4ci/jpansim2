# Check if the current code is being executed during document knitting

Check if the current code is being executed during document knitting

## Usage

``` r
.is_knitting()
```

## Value

TRUE if the whole document is being knitted. FALSE if running in chunk
in RStudio, or otherwise outside of a knitr
