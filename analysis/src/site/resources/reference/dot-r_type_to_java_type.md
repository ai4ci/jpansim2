# Map R vector type to Java type name is the column name. It will be a lowerCamelCase most likely types is the list of classes. They will be UpperCamelCase

Map R vector type to Java type name is the column name. It will be a
lowerCamelCase most likely types is the list of classes. They will be
UpperCamelCase

## Usage

``` r
.r_type_to_java_type(x, name, types)
```
