if (!requireNamespace("utils")) message("Package `utils` must be installed.") # depends on utils
if (!requireNamespace("usethis")) message("Package `usethis` must be installed.") # depends on usethis
if (!requireNamespace("tidyr")) message("Package `tidyr` must be installed.") # depends on tidyr
if (!requireNamespace("tibble")) message("Package `tibble` must be installed.") # depends on tibble
if (!requireNamespace("systemfonts")) message("Package `systemfonts` must be installed.") # depends on systemfonts
if (!requireNamespace("svglite")) message("Package `svglite` must be installed.") # depends on svglite
if (!requireNamespace("stringr")) message("Package `stringr` must be installed.") # depends on stringr
if (!requireNamespace("stats")) message("Package `stats` must be installed.") # depends on stats
if (!requireNamespace("scales")) message("Package `scales` must be installed.") # depends on scales
if (!requireNamespace("rsvg")) message("Package `rsvg` must be installed.") # depends on rsvg
if (!requireNamespace("rstudioapi")) message("Package `rstudioapi` must be installed.") # depends on rstudioapi
if (!requireNamespace("rmarkdown")) message("Package `rmarkdown` must be installed.") # depends on rmarkdown
if (!requireNamespace("rlang")) message("Package `rlang` must be installed.") # depends on rlang
if (!requireNamespace("readr")) message("Package `readr` must be installed.") # depends on readr
if (!requireNamespace("rappdirs")) message("Package `rappdirs` must be installed.") # depends on rappdirs
if (!requireNamespace("ragg")) message("Package `ragg` must be installed.") # depends on ragg
if (!requireNamespace("purrr")) message("Package `purrr` must be installed.") # depends on purrr
if (!requireNamespace("pdftools")) message("Package `pdftools` must be installed.") # depends on pdftools
if (!requireNamespace("officer")) message("Package `officer` must be installed.") # depends on officer
if (!requireNamespace("magrittr")) message("Package `magrittr` must be installed.") # depends on magrittr
if (!requireNamespace("knitr")) message("Package `knitr` must be installed.") # depends on knitr
if (!requireNamespace("huxtable")) message("Package `huxtable` must be installed.") # depends on huxtable
if (!requireNamespace("htmltools")) message("Package `htmltools` must be installed.") # depends on htmltools
if (!requireNamespace("grDevices")) message("Package `grDevices` must be installed.") # depends on grDevices
if (!requireNamespace("ggplot2")) message("Package `ggplot2` must be installed.") # depends on ggplot2
if (!requireNamespace("fs")) message("Package `fs` must be installed.") # depends on fs
if (!requireNamespace("flextable")) message("Package `flextable` must be installed.") # depends on flextable
if (!requireNamespace("dplyr")) message("Package `dplyr` must be installed.") # depends on dplyr
if (!requireNamespace("digest")) message("Package `digest` must be installed.") # depends on digest
if (!requireNamespace("base64enc")) message("Package `base64enc` must be installed.") # depends on base64enc
try(source("R/import-standalone-cache.R"))
try(source("R/import-standalone-file-output-utils.R"))
try(source("R/import-standalone-ggplot-utils.R"))
